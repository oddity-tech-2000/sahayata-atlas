services:
  - type: web
    name: sahayata-atlas
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /api/v1/health
    autoDeployTrigger: commit
    maxShutdownDelaySeconds: 30
    envVars:
      - key: LOG_LEVEL
        value: info
